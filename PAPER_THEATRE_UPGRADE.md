# Fox → Desert：纸剧场转场重构

## 当前实现

本轮依据用户提供的五阶段执行文档，重构草原至沙漠的局部演绎。仅针对桌面，不扩展全站纸艺风格。狐狸、小王子、地表、路径继续使用真实 3D；文字和导航继续保留在 DOM。

1. **麦田建立**：700 组近景双秆穗粒模型、4000 个中景分粒穗模型、4500 个远景简化穗模型（满质量配置）。三档实例化，近处暖金/棕黄/橄榄/枯秆混色，中心通路留空，远处收口。穗头不再是普通尖草叶。
2. **风被看见**：t=9.55 起，先两条手绘笔触风线、最多六片叶子；t=9.86 后增加笔触与草屑。风线采用用户允许的方案 B：曲线带状几何、粗细变化与颗粒断续，不新增素材类别。
3. **布景被牵动**：左右各三组纸板麦穗，顶部两云两星；重复卡片共享几何、材质和图集。底部固定轻摆、错相抽动、分组撤离；吊绳跟随真实孔位。
4. **草转沙**：挂件先提升退场，麦穗从左右分批向外下方抽走；真实麦田依据固定实例种子逐批倒伏和缩小，不再使用屏幕噪点消隐。地面同步变色、形成沙丘与细密沙纹。草叶逐渐干燥缩小，沙石在同一路径接管；最多 48 个逻辑碎片，实测活跃峰值 42。
5. **告别与停留**：狐狸从 t=10.13 开始离开中心，小王子在 t=10.43 后进入，t=10.98 完成交接。t=11.03 后纸艺整组隐藏，t=11.05 进入原有阅读停留点。暮蓝天空、克制的沙粒和小王子构成结尾。

原暂停/继续/回放功能卡片未恢复，沿用侧边章节选择和末尾回放。参考图中的额外文字便签未添加，以保留原叙事文案并避免重复文字。

## 景深、光色与性能

FoxDepthOfField 使用一次场景渲染附带深度纹理，再做克制的 24 样本合成；焦点从狐狸移向小王子。仅 t=9.12～11.8 有景深效果，其他时刻直接渲染，离开 Fox 场景后卸载。UI 不进入渲染目标。

离屏分辨率上限为 1.5 DPR，并跟随现有自动降 DPR。低质量模式降低模糊强度。不新增依赖包。参考 Three.js DepthTexture 文档：https://threejs.org/docs/pages/DepthTexture.html 。

修正天空、太阳及沙粒自定义材质的颜色输出，使离屏合成与直接渲染一致。暖侧光、冷填充和角色脚下柔和接触阴影强化主体落地感。接触阴影是地面材质中的解析近似，本轮没有开启整场动态阴影贴图。

前景随桌面画幅调整边缘位置，镜头鼠标视差减弱，避免宽屏时一侧布景越过文字或完全离框。旧远处残骸缩小并移至远景，避免抢占沙漠留白。

## 素材保留

原来的 9 张 PNG 与使用说明均保留，manifest.json 中全部 10 项 SHA256 验证通过。没有生成新风格图片，也没有改写原图像素。

- 核心麦穗/云/星仍复用既有 cards-atlas.png 与轮廓网格。
- 草叶与沙石无损复制至 public/textures/paper，使用在原图内手工限定的轮廓网格与 UV。
- 衍生图集仍为原先带棋盘背景的 RGB 文件，网格裁剪与材质剔除排除背景。没有将其描述为透明 PNG。

## 验证与实际边界

通过 node scripts/test_paper_motion.mjs、npx.cmd tsc --noEmit、npm.cmd run build。

Chrome + Playwright（当前无 Browser 插件）验证 1440×1000 和 1920×1080：
- 页面标题与入口、Fox 和 B612 侧边导航；
- 七个时间点截图、无框架错误覆盖、无页面/控制台错误；
- 阅读停留、向下滚动继续并停在 Desert；
- 暂停冻结、减少动态效果、相同时间步重播一致；
- 同类卡片共享几何，离开该幕后道具卸载；
- 原始文件哈希和正式构建三张贴图 HTTP 加载。

本轮截图及浏览器脚本在 Codex visualizations 目录的 fox-v2 子目录，未加入项目 verification 目录。旧 verification/paper 截图属于上一版。

这是可交互 Three.js 实现，不是将参考设计图铺成背景。相较概念图，保留现有角色造型、DOM 文案、原场景相机流程；没有添加图中暂停卡片或额外便签。中远景穗头有明确几何简化。浏览器使用软件 WebGL，验证功能与画面，不代表真实显卡帧率；本轮未测手机端。


## 2026-09-10 构图与光影精修

以 1672×941 参考图为构图依据，不新增图像资产。Fox 镜头降低并推进，视场角为 34°；进入沙漠恢复 42°，离开 Fox 后恢复进入前视场角。狐狸移至路径中心附近，周围保留清晰区域。

六组前景麦穗按三个深度排列，最近一层裁出画框；两朵云、四颗星复用原有材质和几何。宽屏边缘补偿改为仅在超过 1.78 画幅时应用。真实麦田保留三档实例数量，调整簇宽、高度、倾斜、颜色及路径边缘分布。

强化右侧低角度暖光、冷色填充和麦穗边缘暖色；太阳下移放大，天空加入程序化远云与地平线轮廓。地面增加稀疏干秆纹理。景深合成为 24 样本，近景最大模糊半径 9.5 像素、远景 2.6 像素；DOM 文字不参与合成。没有新增整场阴影贴图。

温柔风线与五片前奏叶提前出现；前奏叶具有独立连续轨迹，避免在 t=9.55 切换风阶段时突然消失并重现。增加该边界的连续性回归检查。

本次通过动画脚本、TypeScript、生产构建；1672×941 与 1920×1080 浏览器检查通过阅读停留、滚动推进至沙漠、暂停冻结、减少动态效果、重播一致性、共享几何、场景卸载和 FOV 恢复，页面与控制台无错误。截图和脚本保存在 Codex visualizations 的 fox-composition 目录。软件 WebGL 检查不代表实际显卡帧率；未做手机适配。
